import Foundation

/// Top-level namespace + version marker for the Vocateca core library.
public enum Vocateca {
    /// Marketing version of the native v2 build.
    public static let version = "2.0.0"
}
