PyQt6>=6.6
APScheduler>=3.10
watchdog>=4.0
feedparser>=6.0
httpx>=0.27
pyyaml>=6.0
pydantic>=2.6
beautifulsoup4>=4.12
lxml>=5.0
# Transitive — pinned explicitly so py2app bundles them
sniffio>=1.3
anyio>=4.0
h11>=0.14
httpcore>=1.0
pytz>=2024.1
tzlocal>=5.0
# Security: hardened XML parser used in OPML import (defuses XXE + bombs)
defusedxml>=0.7
# Optional: PDF bulk-export (core.bulk_export). Markdown/JSON work without it.
fpdf2>=2.7
# Optional: speaker diarization (core.diarize, off by default). Apache-2.0;
# also needs a one-time segmentation+embedding model download.
# sherpa-onnx>=1.10
# Optional: MCP stdio server (core.mcp_server, `cli.py mcp`). Lets an LLM client
# drive the app; not needed for normal use.
# mcp>=1.0
